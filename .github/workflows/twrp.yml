name: Recovery Build Final Force

on:
  workflow_dispatch:

env:
  MANIFEST_URL: "https://github.com/minimal-manifest-twrp/platform_manifest_twrp_aosp.git"
  MANIFEST_BRANCH: "twrp-12.1"
  DEVICE_TREE_URL: "https://github.com/Yash499-cloud/Il2cppdumper"
  DEVICE_TREE_BRANCH: "main"
  DEVICE_NAME: "TECNO_KI8"

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Cleanup & Environment
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc
          sudo apt-get update
          sudo apt-get install -y gperf gcc-multilib gcc-10-multilib g++-multilib g++-10-multilib libc6-dev libncurses5-dev x11proto-core-dev libx11-dev lib32z1-dev libgl1-mesa-dev libxml2-utils xsltproc unzip fontconfig libncurses-dev git python3
          mkdir -p ~/bin && curl https://storage.googleapis.com/git-repo-downloads/repo > ~/bin/repo
          chmod a+x ~/bin/repo && sudo ln -sf ~/bin/repo /usr/bin/repo

      - name: Sync Source
        run: |
          mkdir -p ~/fox && cd ~/fox
          repo init --depth=1 -u ${{ env.MANIFEST_URL }} -b ${{ env.MANIFEST_BRANCH }}
          repo sync -c -j$(nproc --all) --force-sync --no-tags --no-clone-bundle

      - name: Setup Device Tree
        run: |
          cd ~/fox
          # Ab folder ka naam seedha underscore wala rakhenge
          git clone ${{ env.DEVICE_TREE_URL }} -b ${{ env.DEVICE_TREE_BRANCH }} device/tecno/${{ env.DEVICE_NAME }}

      - name: Build Recovery
        run: |
          cd ~/fox
          export ALLOW_MISSING_DEPENDENCIES=true
          source build/envsetup.sh
          
          # Lunch ab seedha kaam karega
          lunch omni_${{ env.DEVICE_NAME }}-userdebug
          
          # Force clean and build
          make clean
          make recoveryimage -j$(nproc --all)
          
          # Collecting outputs
          mkdir -p ~/outputs
          find out/target/product/${{ env.DEVICE_NAME }}/ -name "recovery.img" -exec cp {} ~/outputs/ \;
          find out/target/product/${{ env.DEVICE_NAME }}/ -name "*.zip" -exec cp {} ~/outputs/ \;

      - name: Upload Artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: Recovery-Final-Package
          path: ~/outputs/*
          
